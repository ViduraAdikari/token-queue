const DASHBOARD_NESTED = {
  queue: 'queue',
  agent: 'agent',
}

export const TOKEN_ROUTE_PATHS = {
  GUEST_LOGIN: '/',
  TOKEN_DASHBOARD: {
    main: '/token-dashboard',
    nested: DASHBOARD_NESTED
  }
}